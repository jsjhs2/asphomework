@model SchoolManagementApp.Models.Student

@{
    ViewData["Title"] = "删除学生";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h3 class="card-title mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>删除学生确认
                    </h3>
                </div>
                <div class="card-body">
                    <h4 class="text-danger">您确定要删除以下学生吗？</h4>
                    <div class="alert alert-danger">
                        <i class="fas fa-exclamation-circle me-2"></i>
                        此操作将永久删除该学生的所有信息，包括相关成绩记录，且无法恢复！
                    </div>

                    <dl class="row">
                        <dt class="col-sm-3">学号：</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.RollNumber)</dd>

                        <dt class="col-sm-3">姓名：</dt>
                        <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

                        <dt class="col-sm-3">班级：</dt>
                        <dd class="col-sm-9">
                            @if (Model.Class != null)
                            {
                                <span class="badge bg-primary">@Model.Class.ClassName</span>
                            }
                            else
                            {
                                <span class="badge bg-warning">未分配班级</span>
                            }
                        </dd>

                        <dt class="col-sm-3">出生日期：</dt>
                        <dd class="col-sm-9">@Model.DateOfBirth.ToString("yyyy-MM-dd")</dd>
                    </dl>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="StudentId" />
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-1"></i>返回列表
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash-alt me-1"></i>确认删除
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>